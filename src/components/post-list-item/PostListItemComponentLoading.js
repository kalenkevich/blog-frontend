import React from 'react';
import withStyles from 'react-jss';

export const styles = {};

const PostListItemLoading = () => (
  <div>Loading</div>
);

export default withStyles(styles)(PostListItemLoading);
